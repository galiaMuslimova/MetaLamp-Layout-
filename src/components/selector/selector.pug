include ../counter/counter.pug
include ../drop-buttons/drop-buttons.pug

mixin selector({ items = [{ name: "Взрослые", amount: 0 }, { name: "Дети", amount: 0 }, { name: "Младенцы", amount: 0 }], hasButton = false })
  .selector.js-selector
    .selector__content 
      each item in items
        .selector__row
          .selector__subtitle=item.name
          .selector__counter.js-selector__counter  
            +counter({ amount: item.amount })
    if(hasButton)
      .selector__buttons.js-selector__buttons 
        +drop-buttons()
          
