include ../checkbox/checkbox.pug

mixin checkbox-list({ items = require('@c/expandable-list/expandable-list.json') } = {})
  ul.checkbox-list
    each item in items
      li.checkbox-list__item
        +checkbox(item)