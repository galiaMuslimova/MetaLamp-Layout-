mixin calendar ()
  .drop 
    .calendar
    if(attributes.class=="select_card")
      .row.row_fill.calendar__row
        button.btn.btn_simple(type="reset") очистить
        button.btn.btn_simple.submit(type="button") применить