include ../drop-buttons/drop-buttons.pug

mixin calendar(options)  
  - const id = options.id || ''

  .calendar.js-calendar
    .calendar__dp.js-calendar__dp(class=`calendar__dp_for-${id} js-calendar__dp_for-${id}` )
    .calendar__buttons 
      +drop-buttons()
    