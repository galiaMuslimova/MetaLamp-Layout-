mixin count(count)
  .row.row_fill.count  
    button.count__btn.minus.js-minus(type='button') -
    input.count__num.js-count__num(type="text" value=count)
    button.count__btn.plus.js-plus(type='button') +