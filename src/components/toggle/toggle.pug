mixin toggle({ text = 'Получать спецпредложения', isCheck = false })
  .toggle
    label.toggle__label
      input.toggle__input(type="checkbox" checked=isCheck)
      span.toggle__box
    p.toggle__text= text