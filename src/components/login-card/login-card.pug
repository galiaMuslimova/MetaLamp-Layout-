include ../input/input.pug
include ../radio/radio.pug
include ../toggle/toggle.pug
include ../calendar/calendar.pug

.card.login-card
  .card__inner
    .card__item 
      h1 Регистрация аккаунта
    .card__item
      +input('text', 'Имя')
      +input('text', 'Фамилия')
      +radio(["Мужчина", "Женщина"])
    .card__item
      h3 дата рождения 
      form
        +input("text", "ДД.ММ.ГГГГ")
        +calendar("datepicker_login")(class="select_card")
    .card__item
      h3 данные для входа в сервис
      +input("text", "Email")
      +input("text", "Пароль")
      +toggle("Получать спецпредложения")
    .card__item
      button.btn.btn_link подобрать номер
        span.icon-arrow
    .card__item
      .row.row_fill 
        p Уже есть аккаунт на Toxin
        button.btn.btn_white.nav__signin войти


      
