include ../input/input.pug
include ../button/button.pug

.signin-card 
  .signin-card__inner
    .signin-card__item 
      h1.signin-card__title Войти
    .signin-card__item 
      .signin-card__input
        +input("text", "Email")
      .signin-card__input
        +input("text", "Пароль")
    .signin-card__item 
      +button({theme: 'link', text: 'Войти', icon: true})
    .signin-card__item 
      .signin-card__row
        p.signin-card__text Нет аккаунта на Toxin?
        +button({theme: 'white', text: 'создать'})
